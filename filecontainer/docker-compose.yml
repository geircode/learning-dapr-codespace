version: '3.7'

services:
  learning-dapr-filecontainer:
    image: geircodeacr.azurecr.io/learning-dapr-filecontainer:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: learning-dapr-filecontainer-1
    networks:
      - learning-dapr-filecontainer-network      

networks:
  learning-dapr-filecontainer-network:
    name: learning-dapr-filecontainer-network    
    